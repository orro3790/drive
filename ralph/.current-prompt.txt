You are in a Ralph AUDIT loop (iteration 20 of 20).

## Mode: AUDIT (Read-Only)

This is an AUDIT loop. You will:
- READ and ANALYZE the codebase
- WRITE reports to `logs/nightly/2026-02-18/`
- CLOSE beads when done

You will NOT:
- Modify any source code
- Create git branches or commits
- Create PRs
- Use /execute skill

## Environment
- Workspace: drive
- Report directory: logs/nightly/2026-02-18/
- Session log: C:\Users\matto\projects\drive\ralph\sessions\ralph-session-20260218-0056.md
- Status file: C:/Users/matto/projects/drive/ralph/.current-task
- Label filter: nightly


## Workflow

### 1. Check for Interrupted Work

```bash
bd list --assignee "drive" --status in_progress --label nightly --json
```

If found, resume that audit task. Otherwise continue to step 2.

### 2. Triage

```bash
bd list --label nightly --status open --json
```

Pick ONE bead with label 'nightly': status=open.

### 3. Claim

```bash
bd update <id> --claim --assignee "drive"
echo "<id> | <title>" > C:/Users/matto/projects/drive/ralph/.current-task
```

### 4. Read Bead Details

```bash
bd show <id>
```

Understand exactly what audit is requested. The bead description contains:
- What to check
- Where to look
- What format to output

### 5. Perform Audit

Do the audit as described in the bead:
- Use Glob, Grep, Read tools to explore
- Analyze findings
- Take thorough notes

### 6. Write Report

Create the report file:
```bash
mkdir -p logs/nightly/2026-02-18
```

Then use Write tool to create: `logs/nightly/2026-02-18/<report-name>.md`

Report format:
```markdown
# <Audit Title>
Generated: 2026-02-18

## Summary
<1-2 sentence overview>

## Findings
<detailed findings with file paths and line numbers>

## Recommended Actions
<numbered list of specific actions to take>
```

### 7. Log & Close

Log completion:
```
Edit tool:
  file_path: C:\Users\matto\projects\drive\ralph\sessions\ralph-session-20260218-0056.md
  old_string: "---"
  new_string: "---\n\n## Iteration 20 - <id>\n\n**Audit**: <title>\n**Report**: logs/nightly/2026-02-18/<name>.md\n\n---"
```

Close the bead:
```bash
bd close <id> --comment "Report: logs/nightly/2026-02-18/<name>.md"
```

### 8. Exit

One audit per iteration. Loop continues automatically.

## Rules

- ONE audit per iteration
- READ-ONLY: Never modify source code
- NO git operations (no branches, commits, PRs)
- NO /execute skill
- Output goes ONLY to logs/nightly/2026-02-18/
- Be THOROUGH: this runs overnight, use the time well
